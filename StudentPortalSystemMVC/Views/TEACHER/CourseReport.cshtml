@*@model StudentPortalSystemMVC.Models.CourseReportViewModel
    @{
        ViewBag.Title = "Course Report - " + Model.CourseName;
        Layout = "~/Views/Shared/_Layout.cshtml";
    }

    <h2>Course Report for @Model.CourseName</h2>

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">@TempData["Error"]</div>
    }

    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Course Statistics</h4>
            <p><strong>Total Students:</strong> @Model.TotalStudents</p>
            <p><strong>Average Attendance:</strong> @Model.AverageAttendance.ToString("F2")%</p>
            <p><strong>Average Marks:</strong> @Model.AverageMarks.ToString("F2")</p>
        </div>
    </div>

    <p>
        <a href="@Url.Action("Dashboard")" class="btn btn-secondary">Back to Dashboard</a>
    </p>*@


@model StudentPortalSystemMVC.Models.CourseReportViewModel
@{
    ViewBag.Title = "Course Report - " + Model.CourseName;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row justify-content-center">
    <div class="col-md-6 col-lg-5">
        <div class="card p-4 mt-5 shadow-sm bg-light">
            <h2 class="text-center mb-4 text-primary">Course Report for @Model.CourseName</h2>

            @if (TempData["Error"] != null)
            {
                <div class="alert alert-danger text-center">@TempData["Error"]</div>
            }

            <div class="card mb-4 shadow-sm">
                <div class="card-body">
                    <h4 class="card-title text-muted mb-3">Course Statistics</h4>
                    <p><strong>Total Students:</strong> @Model.TotalStudents</p>
                    <p><strong>Average Attendance:</strong> @Model.AverageAttendance.ToString("F2")%</p>
                    <p><strong>Average Marks:</strong> @Model.AverageMarks.ToString("F2")</p>
                </div>
            </div>

            <div class="d-flex justify-content-end">
                <a href="@Url.Action("Dashboard")" class="btn btn-secondary">Back to Dashboard</a>
            </div>
        </div>
    </div>
</div>
