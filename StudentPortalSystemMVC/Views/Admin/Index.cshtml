@*@model IEnumerable<StudentPortalSystemMVC.Models.USER>

    @{
        ViewBag.Title = "Admin - Users";
        Layout = "~/Views/Shared/_Layout.cshtml";
    }

    <h2>Users Management</h2>

    @if (Model != null && Model.Any())
    {
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>EMAIL</th>
                    <th>USERNAME</th>
                    <th>ROLE</th>
                    <th>Status</th>
                    <th>Department</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var USER in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => USER.EMAIL)</td>
                        <td>@Html.DisplayFor(modelItem => USER.USERNAME)</td>
                        <td>@Html.DisplayFor(modelItem => USER.ROLE)</td>
                        <td>@Html.DisplayFor(modelItem => USER.STATUS)</td>
                        <td>@(USER.DepartmentId != null && USER.DEPARTMENT != null ? USER.DEPARTMENT.DEP_NAME : "Not Assigned")</td>
                        <td>
                            @if (USER.STATUS == "Pending")
                            {
                                <a href="@Url.Action("Approve", new { id = USER.ID })" class="btn btn-success btn-sm">Approve</a>
                            }
                            <a href="@Url.Action("Edit", new { id = USER.ID })" class="btn btn-primary btn-sm">Edit</a> |
                            <a href="@Url.Action("Details", new { id = USER.ID })" class="btn btn-info btn-sm">Details</a> |
                            <a href="@Url.Action("Delete", new { id = USER.ID })" class="btn btn-danger btn-sm">Delete</a>
                            <a href="@Url.Action("AssignDepartment", new { id = USER.ID })" class="btn btn-warning btn-sm">Assign Department</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No users found.</p>
    }*@


@model IEnumerable<StudentPortalSystemMVC.Models.USER>

@{
    ViewBag.Title = "Admin - Users";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row justify-content-center">
    <div class="col-12">
        <div class="card p-4 mt-5 shadow-sm bg-light">
            <h2 class="text-center mb-4 text-primary">Users Management</h2>

            @if (Model != null && Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-striped table-bordered">
                        <thead class="table-primary">
                            <tr>
                                <th>EMAIL</th>
                                <th>USERNAME</th>
                                <th>ROLE</th>
                                <th>Status</th>
                                <th>Department</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var USER in Model)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(modelItem => USER.EMAIL)</td>
                                    <td>@Html.DisplayFor(modelItem => USER.USERNAME)</td>
                                    <td>@Html.DisplayFor(modelItem => USER.ROLE)</td>
                                    <td>@Html.DisplayFor(modelItem => USER.STATUS)</td>
                                    <td>@(USER.DepartmentId != null && USER.DEPARTMENT != null ? USER.DEPARTMENT.DEP_NAME : "Not Assigned")</td>
                                    <td>
                                        @if (USER.STATUS == "Pending")
                                        {
                                            <a href="@Url.Action("Approve", new { id = USER.ID })" class="btn btn-success btn-sm me-1">Approve</a>
                                        }
                                        <a href="@Url.Action("Edit", new { id = USER.ID })" class="btn btn-primary btn-sm me-1">Edit</a>
                                        <a href="@Url.Action("Details", new { id = USER.ID })" class="btn btn-info btn-sm me-1">Details</a>
                                        <a href="@Url.Action("Delete", new { id = USER.ID })" class="btn btn-danger btn-sm me-1">Delete</a>
                                        <a href="@Url.Action("AssignDepartment", new { id = USER.ID })" class="btn btn-warning btn-sm">Assign Department</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <p class="text-center text-muted">No users found.</p>
            }
        </div>
    </div>
</div>