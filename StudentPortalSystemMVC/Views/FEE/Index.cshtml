@model IEnumerable<StudentPortalSystemMVC.Models.FEE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AMOUNT)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FEE_STATUS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.COURS.COURSE_NAME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.STUDENT.ROLL_NO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AMOUNT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FEE_STATUS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.COURS.COURSE_NAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.STUDENT.ROLL_NO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FEE_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FEE_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FEE_ID })
        </td>
    </tr>
}

</table>
